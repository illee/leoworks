# GitHub Pages 설정 — 내부 기획/설계 문서는 공개 사이트에서 제외(리포엔 보존)
exclude:
  - docs/

# ⚠️ Jekyll은 `.`으로 시작하는 파일·폴더를 기본적으로 게시하지 않는다.
#    명시하지 않으면 /.well-known/assetlinks.json 이 404가 되어 Android App Link 검증이 실패한다.
include:
  - .well-known
